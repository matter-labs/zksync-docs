---
title: Debug JSON-RPC API
description: Methods useful for debugging purposes with ZKsync Chains.
github: https://github.com/matter-labs/zksync-era/blob/main/core/lib/web3_decl/src/namespaces/debug.rs
---

::callout{icon="i-heroicons-exclamation-triangle" color="amber"}
Endpoints for ZKsync OS chains are still under development, and may be unstable.
::

## `debug_traceBlockByHash`

Traces all calls made from a specific block by its L2 hash.

#### Parameters

1. **DATA, 32 bytes** - hash defining the L2 block.
1. **TracerConfig** - Optional configuration for tracing. Refer to the
[TraceConfig documentation](https://geth.ethereum.org/docs/interacting-with-geth/rpc/ns-debug#traceconfig) for more details.

#### Returns

Array of objects, each representing a traced call made from the specified block.

:display-partial{path="zksync-protocol/api/_partials/_trace-object"}

#### Example Request

::content-switcher
---
items: [{
  label: 'curl',
  partial: '_partials/_api/_curl/_debug_trace-block-by-hash'
}, {
  label: 'TypeScript',
  partial: '_partials/_api/_ts/_debug_trace-block-by-hash'
}]
---
::

#### Example Response

:display-partial{path="zksync-protocol/api/_partials/_trace-result"}

---

## `debug_traceBlockByNumber`

Traces all calls made from a specific block by its L2 block number.

#### Parameters

1. **QUANTITY, 8 bytes | TAG** - The number of the block to trace. This can be a hex-encoded number or one of the strings "earliest", "latest", or "pending".
1. **TracerConfig** - Optional configuration for tracing. Refer to the
[TraceConfig documentation](https://geth.ethereum.org/docs/interacting-with-geth/rpc/ns-debug#traceconfig) for more details.

#### Returns

Array of objects, each representing a traced call made from the specified block.

:display-partial{path="zksync-protocol/api/_partials/_trace-object"}

#### Example Request

::content-switcher
---
items: [{
  label: 'curl',
  partial: '_partials/_api/_curl/_debug_trace-block-by-number'
}, {
  label: 'TypeScript',
  partial: '_partials/_api/_ts/_debug_trace-block-by-number'
}]
---
::

#### Example Response

:display-partial{path="zksync-protocol/api/_partials/_trace-result"}

---

## `debug_traceCall`

Traces a call made at a specific block, by block number or hash.

#### Parameters

1. **CallRequest** - The call request to trace, containing fields like `from`, `to`, `data`, and optionally `gas`, `gasPrice`, and `value`.
1. **DATA, 32 bytes | QUANTITY, 8 bytes** - Optional. The block identifier, which can be a block number as a hex-encoded number or a block hash.
If not specified, the latest block is used.
1. **TracerConfig** - Optional. Configuration options for the trace. For more details, refer to the [TraceConfig documentation](https://geth.ethereum.org/docs/interacting-with-geth/rpc/ns-debug#traceconfig).

#### Returns

Array of objects, each representing a traced call made from the specified block.

:display-partial{path="zksync-protocol/api/_partials/_trace-object"}

#### Example Request

::content-switcher
---
items: [{
  label: 'curl',
  partial: '_partials/_api/_curl/_debug_trace-call'
}, {
  label: 'TypeScript',
  partial: '_partials/_api/_ts/_debug_trace-call'
}]
---
::

#### Example Response

:display-partial{path="zksync-protocol/api/_partials/_trace-result"}

---

## `debug_traceTransaction`

Uses the [EVM's `callTracer`](https://geth.ethereum.org/docs/developers/evm-tracing/built-in-tracers#call-tracer)
to return a debug trace of a specific transaction given by its transaction hash.

#### Parameters

1. **DATA, 32 bytes** - The 32-byte hash of the transaction to trace.
1. **TracerConfig** - Optional. Configuration options for the trace. For more details, refer to the [TraceConfig documentation](https://geth.ethereum.org/docs/interacting-with-geth/rpc/ns-debug#traceconfig).

#### Returns

Array of objects, each representing a traced call made from the specified block.

:display-partial{path="zksync-protocol/api/_partials/_trace-object"}

#### Example Request

::content-switcher
---
items: [{
  label: 'curl',
  partial: '_partials/_api/_curl/_debug_trace-transaction'
}, {
  label: 'TypeScript',
  partial: '_partials/_api/_ts/_debug_trace-transaction'
}]
---
::

#### Example Response

:display-partial{path="zksync-protocol/api/_partials/_trace-result"}

## EraVM Endpoints

The following endpoints are currently only implemented in EraVM chains.
They will be available for ZKsync OS chains in the future.

## `debug_getRawTransaction`

Returns the raw, RLP-encoded data of a single transaction, given its transaction hash.

#### Parameters

1. **DATA, 32 bytes** - The 32-byte hash of the transaction to trace.
1. **TracerConfig** - Optional. Configuration options for the trace. For more details, refer to the [TraceConfig documentation](https://geth.ethereum.org/docs/interacting-with-geth/rpc/ns-debug#traceconfig).

#### Returns

1. **DATA** - Full serialized transaction exactly as it was submitted to the network. The format matches Ethereum’s standard raw transaction encoding.

#### Example Request

```sh
curl --request POST \
  --url https://mainnet.era.zksync.io/ \
  --header 'Content-Type: application/json' \
  --data '{
      "jsonrpc": "2.0",
      "id": 1,
      "method": "debug_getRawTransaction",
      "params": ["0x4bd0bd4547d8f8a4fc86a024e54558e156c1acf43d82e24733c6dac2fe5c5fc7"]
    }'
```

#### Example Response

```json
{
  "jsonrpc": "2.0",
  "result": ["0xf8678084342770c182520894658bdf435d810c91414ec09147daa6db624063798203e880820a95a0af5fc351b9e457a31f37c84e5cd99dd3c5de60af3de33c6f4160177a2c786a60a0201da7a21046af55837330a2c52fc1543cd4d9ead00ddf178dd96935b607ff9b]
}
```

---

## `debug_getRawTransactions`

Returns a list of raw, RLP-encoded transactions for a specific block.

#### Parameters

##### ZKsync OS

:display-partial{path="zksync-protocol/api/_partials/_block-id"}

##### EraVM

1. **QUANTITY, 8 bytes | TAG** - The number of the block to trace. This can be a hex-encoded number or one of the strings "earliest", "latest", or "pending".
1. **TracerConfig** - Optional configuration for tracing. Refer to the
[TraceConfig documentation](https://geth.ethereum.org/docs/interacting-with-geth/rpc/ns-debug#traceconfig) for more details.

#### Returns

1. **DATA** - Array of full serialized transaction exactly as it was submitted to the network. The format matches Ethereum’s standard raw transaction encoding.

#### Example Request

```sh
curl --request POST \
  --url https://mainnet.era.zksync.io/ \
  --header 'Content-Type: application/json' \
  --data '{
      "jsonrpc": "2.0",
      "id": 1,
      "method": "debug_getRawTransactions",
      "params": ["0x1d1551e"]
    }'
```

#### Example Response

```json
{
  "jsonrpc": "2.0",
  "result": [
  "0xf8678084342770c182520894658bdf435d810c91414ec09147daa6db624063798203e880820a95a0af5fc351b9e457a31f37c84e5cd99dd3c5de60af3de33c6f4160177a2c786a60a0201da7a21046af55837330a2c52fc1543cd4d9ead00ddf178dd96935b607ff9b",
  "0xf86b0185030d40f680825208943535353535353535353535353535353535353535880de0b6b3a76400008025a071c349c30c1b8ed17c2c6b85b2b2a5fd888de0b7c3c6d678e912fcd894bc0e4ca07d893d0cc7899ac76549e18006e2beef6ccbc6d10a6c4a9173aa9cbbf137d61d",
  "0xf86c028477359400825208949876543210abcdef1234567890abcdef123456789088015f90c9b3af00008026a0a392e91d9a0b21fd1582d96b11aa80cf9874d2e41798e169f7cf13c5152a8f75a037ee9f64f73e617011e44f61f601b6ed553b68d80c10b5a1d1e24879a2c6db5b"
  ]
}
```

---

<!--
UNIMPLEMENTED
TODO: add these back and complete once implemented

## ZKsync OS Endpoints

The following endpoints are only available on the ZKsync OS Server.
They are not available for EraVM chains.

## `debug_getRawHeader`

Returns an [RLP-encoded](https://ethereum.org/developers/docs/data-structures-and-encoding/rlp/) header.

#### Parameters

:display-partial{path="zksync-protocol/api/_partials/_block-id"}

#### Returns

The raw bytes of the block header.

#### Example Request

#### Example Response

```json

```

## `debug_getRawBlock`

Returns an [RLP-encoded](https://ethereum.org/developers/docs/data-structures-and-encoding/rlp/) block.

#### Parameters

:display-partial{path="zksync-protocol/api/_partials/_block-id"}

#### Returns

The raw bytes of the block.

#### Example Request

#### Example Response

```json

```

## `debug_getRawReceipts`

Returns an array of [EIP-2718](https://eips.ethereum.org/EIPS/eip-2718) binary-encoded receipts.

#### Parameters

#### Returns

#### Example Request

#### Example Response

```json

```

## `debug_traceBlock`

Returns a full stack trace of all invoked opcodes of all
transactions that were included in this block.
This expects an [RLP-encoded](https://ethereum.org/developers/docs/data-structures-and-encoding/rlp/) block.
Note, the parent of this block must be present, or it will fail. For the second parameter see
[`GethDebugTracingOptions`](https://github.com/alloy-rs/alloy/blob/155dcb1db839ec4c3d58e74bb62d5fd5aeb23a2e/crates/rpc-types-trace/src/geth/mod.rs#L476)
reference.

#### Parameters

#### Returns

#### Example Request

#### Example Response

:display-partial{path="zksync-protocol/api/_partials/_trace-result"}

## `debug_traceCallMany`

The `debug_traceCallMany` method lets you run an `eth_callMany` within the context of the
given block execution using the final state of parent block as the base followed by n transactions.

The first argument is a list of bundles. Each bundle can overwrite the block headers.
This will affect all transaction in that bundle.
`BlockNumber` and `transaction_index` are optional.

`Transaction_index` specifies the number of tx in the block to replay and -1 means all transactions should be replayed.

The trace can be configured similar to `debug_traceTransaction`.
State override apply to all bundles.

This methods is similar to many `eth_callMany`, hence this returns nested lists of traces.
Where the length of the outer list is the number of bundles and the length of the inner list (`Vec<GethTrace>`) is the number of transactions in the bundle.

#### Parameters

#### Returns

#### Example Request

#### Example Response

```json

```

## `debug_chainConfig`

Returns the current chain config.

#### Parameters

#### Returns

#### Example Request

#### Example Response

```json

```

## `debug_codeByHash`

Returns the code associated with a given hash at the specified block ID.
If no block ID is provided, it defaults to the latest block.

#### Parameters

#### Returns

#### Example Request

#### Example Response

```json

```
 -->