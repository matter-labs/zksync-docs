---
title: Local Setup
description: Setup a local environment for testing interoperable ZKsync chains.
---

To setup a local environment for testing L2 -> L2 interoperability, follow the steps below.

::callout{icon="i-heroicons-light-bulb"}
Make sure you have [Rust](https://rust-lang.org/tools/install/) and version
`1.5.1` of `anvil` via
[`foundry`](https://getfoundry.sh/introduction/installation) installed.
::

To setup a local environment for testing,
clone the [zksync-os-server repo](https://github.com/matter-labs/zksync-os-server):

```bash
git clone https://github.com/matter-labs/zksync-os-server.git
```

Then run the command below to start a local environment with two L2 chains that have `v31` interop enabled:

```bash
./run_local.sh ./local-chains/v31.0/multi_chain
```

The first time running this can take a few minutes for the dependencies to
compile.

You should now have three local chains running:

- a local L1 chain running at port `8545`
- a local L2 chain running at port `3050` (chain ID `6565`)
- a second local L2 chain running at port `3051` (chain ID `6566`)

Note that once you end this process, the history of each chain will be
completely erased.
These are in-memory nodes, so they do no persist any
storage of the chains.
