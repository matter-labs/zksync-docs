# Glossary and Terminology Guardrails

Use canonical terms below. Avoid inconsistent variants in new docs changes.

| Canonical Term | Avoid | Notes |
| --- | --- | --- |
| `ZKsync` | mixed-case or lowercase spelling variants | Use official product spelling. |
| `ZK Stack` | `ZKstack`, `zk stack` | Keep spacing/casing consistent. |
| `ZKsync Era` | `Era chain` (ambiguous) | Use full term on first mention. |
| `EraVM` | `EVM++`, `custom EVM` | Avoid unofficial labels. |
| `L2 chain` | `sidechain` (incorrect context) | Use accurate architecture term. |
| `smart contract` | `program` (when Solidity/EVM context) | Match ecosystem language. |
| `Prividium™` | `ZKsync Prividium`, `Prividium` (without ™) | This is a licensed product, so in most cases it should have ™ . |

## Terminology Rules

- Use one canonical term per concept within a page.
- If a request introduces a new term, define it once before broad use.
- Do not introduce unofficial synonyms for protocol components.
- Always use `ZKsync` (exact casing) in prose and headings.
